spring:
  main:
    allow-bean-definition-overriding: true
  application:
    name: user
#  profiles:
#    active: local
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        file-extension: yaml